name: CI/CD Pipeline 

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # Tests and setup for Laravel
      - name: Setup PHP with extensions
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: mbstring, pdo, pdo_mysql, curl, gd, xml, zip
          coverage: none

      - name: Install Composer dependencies (Laravel)
        run: composer install --prefer-dist --no-progress --no-suggest

      - name: Setup Laravel Environment
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: Run Laravel migrations (using SQLite for testing)
        run: |
          touch database/database.sqlite
          php artisan migrate

      - name: Run Laravel tests
        run: php artisan test

      # Tests for Vue.js
      - name: Install Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install npm dependencies (Vue.js)
        run: npm install

      - name: Run Vue.js tests
        run: npm run test

      # Login to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Build and push Docker images
      - name: Build and push Docker images (Laravel)
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./path-to-laravel/Dockerfile # Update path to your Laravel Dockerfile
          push: true
          tags: yourusername/laravel-app:latest # Update image name and tag

      - name: Build and push Docker images (Vue.js)
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./path-to-vuejs/Dockerfile # Update path to your Vue.js Dockerfile
          push: true
          tags: yourusername/vuejs-app:latest # Update image name and tag 
